# لیفتر - نسخه تکنیسین

یک اپلیکیشن React Native برای مدیریت خدمات فنی با رابط کاربری فارسی و اتصال به API لاراول.

## ویژگی‌ها

- ✅ **احراز هویت کامل**: 
  - ورود با رمز عبور
  - ورود با کد OTP (۶ رقمی)
  - مدیریت توکن و نشست
  - خروج امن از سیستم
  
- 🎨 **رابط کاربری فارسی**: طراحی مدرن و کاربرپسند
- 🔐 **امنیت**: ذخیره‌سازی امن توکن، اعتبارسنجی خودکار
- 📱 **پشتیبانی کامل از React Native**: اجرا بر روی iOS و Android
- 🌐 **اتصال به Laravel API**: یکپارچگی کامل با بکند لاراول

## رنگ‌بندی

- **Federal Blue** (#03045E): متن‌های اصلی
- **Honolulu Blue** (#0077B6): رنگ اصلی و دکمه‌ها
- **Pacific Cyan** (#00B4D8): آکسان‌ها
- **Non Photo Blue** (#90E0EF): پس‌زمینه‌های روشن
- **Light Cyan** (#CAF0F8): پس‌زمینه اصلی

## ساختار پروژه

```
my-expo-app/
├── App.tsx                    # کامپوننت اصلی و مدیریت ناوبری
├── config/
│   └── api.config.ts          # تنظیمات API (URL, endpoints)
├── context/
│   └── AuthContext.tsx        # مدیریت وضعیت احراز هویت
├── services/
│   ├── api.ts                 # کلاینت Axios با interceptors
│   └── authService.ts         # سرویس‌های احراز هویت
├── types/
│   └── index.ts               # تعریف TypeScript Types
├── screens/
│   ├── WelcomeScreen.tsx      # صفحه خوش‌آمدگویی
│   ├── LoginScreen.tsx        # ورود با شماره تلفن
│   ├── PasswordScreen.tsx     # ورود با رمز عبور / OTP
│   ├── VerificationScreen.tsx # تأیید کد ۶ رقمی OTP
│   └── DashboardScreen.tsx    # داشبورد اصلی
└── docs/
    ├── AUTHENTICATION.md      # مستندات کامل احراز هویت
    ├── QUICK_START.md         # راهنمای شروع سریع
    └── API_REFERENCE.md       # مرجع API
```

## نصب و راه‌اندازی

### 1. نصب وابستگی‌ها

```bash
npm install
```

### 2. تنظیم API

فایل `config/api.config.ts` را ویرایش کنید:

```typescript
export const API_CONFIG = {
  BASE_URL: 'http://127.0.0.1:8000/api', // آدرس API خود را وارد کنید
  TIMEOUT: 10000,
};
```

**نکات مهم برای تنظیم URL:**
- **iOS Simulator**: `http://localhost:8000/api` یا `http://127.0.0.1:8000/api`
- **Android Emulator**: `http://10.0.2.2:8000/api`
- **دستگاه فیزیکی**: `http://YOUR_LOCAL_IP:8000/api` (مثلاً `http://192.168.1.100:8000/api`)

### 3. راه‌اندازی بکند Laravel

```bash
cd /path/to/laravel
php artisan serve
```

### 4. اجرای پروژه

```bash
npm start
# یا
expo start
```

سپس:
- برای Android: کلید `a` را فشار دهید
- برای iOS: کلید `i` را فشار دهید
- برای دستگاه فیزیکی: QR کد را با اپلیکیشن Expo Go اسکن کنید

## API Endpoints

اپلیکیشن با API‌های زیر ارتباط برقرار می‌کند:

| Endpoint | Method | نیاز به احراز هویت | توضیحات |
|----------|--------|-------------------|---------|
| `/technician/login` | POST | خیر | ورود با رمز عبور |
| `/technician/send-otp` | POST | خیر | ارسال کد OTP |
| `/technician/verify-otp` | POST | خیر | تایید کد OTP و ورود |
| `/technician/logout` | POST | بله | خروج از سیستم |
| `/technician/check-auth` | GET | بله | بررسی وضعیت احراز هویت |

## استفاده از Context احراز هویت

```typescript
import { useAuth } from './context/AuthContext';

function MyComponent() {
  const { 
    isAuthenticated,  // وضعیت ورود کاربر
    technician,       // اطلاعات تکنسین
    login,            // ورود با رمز عبور
    sendOtp,          // ارسال OTP
    verifyOtp,        // تایید OTP
    logout            // خروج
  } = useAuth();

  // استفاده از متدها
  await login('09123456789', 'password');
  await sendOtp('09123456789');
  await verifyOtp('09123456789', '123456');
  await logout();
}
```

## مستندات

برای اطلاعات بیشتر به فایل‌های زیر مراجعه کنید:

- 📖 [AUTHENTICATION.md](docs/AUTHENTICATION.md) - مستندات کامل سیستم احراز هویت
- 🚀 [QUICK_START.md](docs/QUICK_START.md) - راهنمای شروع سریع
- 📚 [API_REFERENCE.md](docs/API_REFERENCE.md) - مرجع کامل API

## تکنولوژی‌های استفاده شده

- **React Native** - فریمورک موبایل
- **Expo** - پلتفرم توسعه
- **TypeScript** - Type safety
- **Tailwind CSS (NativeWind)** - استایل‌دهی
- **Axios** - HTTP client
- **AsyncStorage** - ذخیره‌سازی محلی
- **React Context** - مدیریت state

## مشکلات رایج

### خطای Network Request Failed

برای Android Emulator از آدرس `http://10.0.2.2:8000/api` استفاده کنید.

### خطای CORS

در Laravel، فایل `config/cors.php` را چک کنید:

```php
'paths' => ['api/*'],
'allowed_origins' => ['*'],
```

### OTP دریافت نمی‌شود

- در حالت توسعه، کد OTP در یک Alert نمایش داده می‌شود
- مطمئن شوید که `app.debug = true` در Laravel است

## لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.
